enum RoomStatus {
  WAITING
  PLAYING
  FINISHED
}

enum GameStatus {
  PREPARING
  IN_PROGRESS
  PAUSED
  FINISHED
  CANCELLED
}

enum Role {
  USER
  ADMIN
}

enum UserStatus {
  ONLINE
  OFFLINE
  IN_GAME
}